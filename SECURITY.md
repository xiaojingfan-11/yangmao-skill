# Security

Report vulnerabilities privately through GitHub Security Advisories. Do not open public issues containing credentials, private API details, user data, production URLs, or exploit steps against a live service.

The client accepts only an HTTPS API origin, validates bounded inputs and structured responses, and never constructs destination links.
